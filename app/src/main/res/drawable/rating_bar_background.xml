<?xml version="1.0" encoding="UTF-8"?>
<layer-list
    xmlns:android="http://schemas.android.com/apk/res/android">
    <item
        android:id="@android:id/background">
        <shape>
            <corners
                android:radius="30dp" />
            <gradient
                android:startColor="@color/rating_back"
                android:centerColor="@color/rating_back"
                android:centerY="0.50"
                android:endColor="@color/rating_back"
                android:angle="270" />
        </shape>
    </item>
    <item
        android:id="@android:id/progress">
        <clip>
            <shape>
                <corners
                    android:radius="30dp" />
                <gradient
                    android:startColor="@color/colorPrimary"
                    android:endColor="@color/colorPrimary"
                    android:angle="90" />
            </shape>
        </clip>
    </item>
</layer-list>